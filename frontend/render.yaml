services:
  - type: web
    name: ticket-classifier
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: MODEL_PATH
        value: ticket_classifier.pt
      - key: PYTHON_VERSION
        value: 3.10.0
